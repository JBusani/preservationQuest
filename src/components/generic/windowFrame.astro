---
interface Props{
    pageTitle: string;
}
const {
    pageTitle,

} = Astro.props;

---
    <article id="windowFrame" class="windowFrame">
    
        <slot />
    
        
    </article>

<style>
    .windowFrame{
        position: relative;
        top: -2rem;
        bottom: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        margin: 0px auto;
        height: 100%;
        min-height: 480px;
        max-height: 700px;
        width: 100%;
        max-width: 75%;
        background-color: var(--windowbg);
        border: 2px solid black;
        border-radius: 10px 10px 0px 0px;
    }
</style>